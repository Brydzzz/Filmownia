#include "guest.h"
// #include "review.cpp"

class Logged : public Guest
{
    // std::vector<Review> reviews;
public:
    // Logged(std::vector<Review> user_reviews) : reviews(user_reviews){}
    // std::vector<Review> getReviews();
    // std::ostream showReviews();
    // void writeReview(Review new_review);
    // void deleteReview(Review to_delete);
};